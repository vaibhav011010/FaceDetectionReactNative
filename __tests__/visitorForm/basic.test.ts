describe("Basic Offline Test", () => {
    it("should work without importing complex modules", () => {
      expect(true).toBe(true);
    });
  
    it("should handle basic math", () => {
      expect(2 + 2).toBe(4);
    });
  });